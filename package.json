{
  "name": "@bnbpay/x402flex",
  "version": "0.1.0",
  "private": false,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "description": "HTTP 402 (x402 Flex) helpers for building responses and verifying settlements on Node/Express servers.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -w -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@bnbpay/sdk": "file:../sdk-ts",
    "ethers": "^6.15.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^24.10.1",
    "typescript": "^5.9.2",
    "vitest": "^4.0.8"
  }
}
