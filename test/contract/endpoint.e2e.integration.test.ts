import { describe, it } from 'vitest';

describe.skip('endpoint e2e integration (nightly)', () => {
  it('runs challenge -> payment -> retry success flow', async () => {
    // Nightly integration scaffold.
  });
});

