import { describe, it } from 'vitest';

describe.skip('vector parity (nightly)', () => {
  it('matches docs/X402_FLEX_REFERENCE_VECTORS', async () => {
    // Nightly integration scaffold.
  });
});

