import { describe, it } from 'vitest';

describe.skip('openapi route drift (nightly)', () => {
  it('compares openapi spec with implemented route handlers', async () => {
    // Nightly integration scaffold.
  });
});

